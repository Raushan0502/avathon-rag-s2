# Copy this file to .env and fill in real values. .env is gitignored and never committed.

# Exactly one of these is required for the generation step (Step 5).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Which provider to use: "anthropic" or "openai"
LLM_PROVIDER=anthropic
